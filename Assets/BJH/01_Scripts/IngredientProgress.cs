using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// ingredient 상태에 따라
// timer bar를 변경

// Ingredient.cs의 SLICE_PROCESS 프로퍼티를 활용
// 범위값에 따라서 게이지 변경
public class IngredientProgress : MonoBehaviour
{



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
